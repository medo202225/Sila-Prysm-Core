### Changed
- `pingHandler`: Use service context instead of background context.
- `monitor`: Move the `Processed aggregated attestation` log to debug.

## Fixed
- Fix correct head, source and target behaviour when using the `--monitor-indices` flag.

