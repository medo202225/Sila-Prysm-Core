### Added

- REST validator client now implements `PayloadAttestationData` and `SubmitPayloadAttestation` against the beacon node `/sila/v1/validator/payload_attestation_data/{slot}` and `/sila/v1/beacon/pool/payload_attestations` endpoints (previously returned "not implemented").
